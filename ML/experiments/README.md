# ml/experiments/

Notebooks experimentales. **No producción.** El código maduro se gradúa a
`ml/training/<modelo>/train.py`.

| Notebook | Descripción |
|---|---|
| `xgboost_v3_diversity.ipynb` | XGBoost con diversity features (copiado desde ML/) |
| `tft_intra_ciclo.ipynb` | Temporal Fusion Transformer — Fase F (futuro) |

> Los notebooks de EDA base están en `ml/experiments/` también.
> El notebook original `ML/eda_milpin.ipynb` se mueve aquí.
