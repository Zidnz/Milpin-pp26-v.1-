# FASE B COMPLETADA — este archivo debe eliminarse manualmente.
# El módulo real está en ml/inference/anomaly_detector.py
#
# Si ves este archivo y algo lo importa, es un import residual no migrado.
# Usa: from ml.inference.anomaly_detector import ...
#
# Para eliminar este archivo en Windows:
#   del backend\core\anomaly_detector.py

raise ImportError(
    "backend/core/anomaly_detector.py fue reemplazado en Fase B (2026-05-16). "
    "Importa desde: from ml.inference.anomaly_detector import ..."
)
