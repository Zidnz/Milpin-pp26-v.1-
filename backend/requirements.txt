fastapi==0.115.0
uvicorn[standard]==0.30.6

# Base de datos
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
aiosqlite==0.20.0

# Migraciones
alembic==1.14.0
geoalchemy2==0.17.1

# Autenticación JWT
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# Variables de entorno
python-dotenv==1.0.1

# ML / Agronómico
pandas==2.2.3
shapely==2.0.6

numpy==1.26.4
scipy==1.12.0

scikit-learn==1.4.2
xgboost==2.1.1

# numba, llvmlite, shap eliminados — no se usan en el backend de producción.
# shap y scipy solo aparecen en ml/training/ (scripts offline).
# Reinstalar localmente si se necesitan para entrenamiento: pip install numba shap s